let fs,chalk,path,exitCodes;_73d‍.x([["getArrayHtmlPhpPaths",()=>getArrayHtmlPhpPaths],["findFilesInDir",()=>findFilesInDir],["getCssReferencedInHtml",()=>getCssReferencedInHtml],["getIdsAndClassesFromString",()=>getIdsAndClassesFromString]]);_73d‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_73d‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);_73d‍.w("path",[["default",["path"],function(v){path=v}]]);_73d‍.w("./exitCodes",[["default",["exitCodes"],function(v){exitCodes=v}]]);


const readline = require('readline');


       function getArrayHtmlPhpPaths(folderToImplode) {
    const htmlFiles = this.findFilesInDir(folderToImplode, '.html');
    const phpFiles = this.findFilesInDir(folderToImplode, '.php');


    return htmlFiles.concat(phpFiles);
}


/**
 * Find all files recursively in specific folder with specific extension, e.g:
 * findFilesInDir('./src', '.html') => ['./src/a.html','./src/build/index.html']
 * @param  {String} startPath    Relative path to the root folder.
 * @param  {String} filter       Extension name, e.g: '.html' lowercase letters.
 * @return {Array}               Result files with absolute path in an array.
 */
       function findFilesInDir(startPath, filter) {

    var results = [];

    if (!fs.existsSync(startPath)) {
        exitCodes(401, startPath);
        return;
    }

    var files = fs.readdirSync(startPath);
    for (var i = 0; i < files.length; i++) {
        var filename = path.join(startPath, files[i]);
        //filename = filename.toLocaleLowerCase();
        var stat = fs.lstatSync(filename);
        if (stat.isDirectory()) {
            results = results.concat(findFilesInDir(filename, filter)); //recurse
        } else if (filename.indexOf(filter) >= 0) {
            results.push(filename);
        }
    }
    return results;
}

       function getCssReferencedInHtml(filePath) {
    fs.readFile(filePath, {
        encoding: 'utf-8'
    }, function (err, data) {
        if (!err) { // file found
            //console.log('received data: ' + data);
            getIdsAndClassesFromString(data);
        } else {
            _73d‍.g.console.log(err);
        }
    });
}

       function getIdsAndClassesFromString(lineWithCss) {
    _73d‍.g.console.log('linea: ' + lineWithCss);
    let idDoubleNormalComma = lineWithCss.replace(/(id="(.*?)(\"))/g, '')
    _73d‍.g.console.log(/(id="(.*?)(\"))/g.exec(lineWithCss)[2])
}
