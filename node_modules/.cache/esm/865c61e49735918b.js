let fs;_973‍.x([["getIdsFromString",()=>getIdsFromString],["getClassFromString",()=>getClassFromString],["getIdsReferencedInHtml",()=>getIdsReferencedInHtml],["getClassesReferencedInHtml",()=>getClassesReferencedInHtml],["default",()=>_973‍.o]]);_973‍.w("fs",[["default",["fs"],function(v){fs=v}]]);

// eslint-disable-next-line consistent-return
       function getIdsFromString(lineWithCss) {
    const idFound = lineWithCss.match(/(id="(.*?)("))|(id='(.*?)('))|(id=`(.*?)(`))/g);
    if (idFound !== null) {
        return idFound.toString().substr(4, idFound.toString().length - 6);
    }
}

// eslint-disable-next-line consistent-return
       function getClassFromString(lineWithCss) {
    const classFound = lineWithCss.match(/(class="(.*?)("))|(class='(.*?)('))|(class=`(.*?)(`))/g);
    if (classFound !== null) {
        return classFound.toString().substr(7, classFound.toString().length - 8);
    }
}

       function getIdsReferencedInHtml(filePath) {
    const ids = [];
    fs.readFileSync(filePath, 'utf-8').split(/\r?\n/).forEach((line) => {
        if (getIdsFromString(line) !== undefined) {
            ids.push(getIdsFromString(line));
        }
    });
    return ids;
}

       function getClassesReferencedInHtml(filePath) {
    const classes = [];
    fs.readFileSync(filePath, 'utf-8').split(/\r?\n/).forEach((line) => {
        const result = getClassFromString(line);
        if (result !== undefined) {
            classes.push(result.split(' '));
        }
    });
    return classes;
}


_973‍.d(getIdsReferencedInHtml);
